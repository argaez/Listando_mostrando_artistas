<%= form_for @artist do |f| %>
    <div>
        <%= f.label :name %><br>
        <%= f.text_field :name %>
    </div>
    <div>
        <%= f.label :image_url %><br>
        <%= f.text_area :image_url %>
    </div>
   
    <div>
        <%= f.submit class:"btn btn-primary" %>
    </div>

<% end %>